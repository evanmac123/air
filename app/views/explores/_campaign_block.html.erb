<% @campaigns.each do |board| %>
  <div class="campaign tile_container">
      <div class="topic-board-div">
        <%= render partial: "explores/single_campaign", locals: {
          board: board,
          path: stock_board_path(library_slug: board.public_slug)
          } %>
      </div>
  </div>
<% end %>
