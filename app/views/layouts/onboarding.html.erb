<!DOCTYPE html>
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= content_for :meta %>
  <title>Airbo</title>

  <%= javascript_include_tag "https://api.intercom.io/api/js/library.js" %>
  <%= javascript_include_tag 'app-admin' %>
  <%= javascript_include_tag 'app-client-admin' %>
  <%= stylesheet_link_tag 'app-user-progress' %>
  <%= javascript_include_tag 'for-modern-browsers' unless browser.ie8? %>
  <%= split_stylesheet_link_tag "app-client-admin", :media => "all", :split_count => 3 %>

  <%= csrf_meta_tag %>
  <%= render partial: '/shared/mixpanel' %>
  <%= content_for :head%>
</head>

<body class="<%= body_class %> onboarding-body client_admin_main user-onboarding-state-<%= @user_onboarding.state %>" data-id="<%= @user_onboarding.id %>" data-state="<%= @user_onboarding.state %>" data-demo-scheduled="<%= @user_onboarding.demo_scheduled %>" style="-webkit-overflow-scrolling: touch; overflow:visible; ">
  <% unless request.user_agent =~ /Mobile|webOS/ %>
    <%= render partial: 'user_onboardings/partials/header' , locals:{user_onboarding: @user_onboarding}%>
    <main class="main">
    <%= render partial: 'user_onboardings/partials/onboarding_subnav', locals:{user_onboarding: @user_onboarding}%>
  <% end %>
    <div class="core <%= params[:controller] + "-" + params[:action] %>-content">
      <div class="content <%= params[:controller] + "-" + params[:action] %>-content">
        <%= yield %>
      </div>
      <div id="footer">
      </div>
    </div>
  </main>

  </div>

  <% render 'shared/intercom' %>
  <%= render 'user_onboardings/partials/share_modal' if @user_onboarding.id %>
  <% if @user_onboarding.state == @user_onboarding.final_state %>
    <%= render 'user_onboardings/partials/onboarding_complete_modal' %>
  <% end %>

  <%= yield :lightbox %>
  <%= render :partial => 'shared/javascript' %>
  <div id='gradient-triangle'></div>
</body>
</html>
