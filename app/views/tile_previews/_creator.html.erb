<div class="creator">
  <%= image_tag tile.demo.logo(:thumb), class: "horizontal company_logo" %>
  <span>
    Tile 
    <% if tile.creator %>
      by <%= tile.creator.try(:name) %> 
    <% end %>
    of <%= tile.demo.name %>
  </span>
</div>