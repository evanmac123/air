<%content_for :head do%>
  <%= render partial: 'meta_tags', locals: { tile: @tile } %>
<%end%>
<%= render partial: 'tile_preview', locals: { tile: @tile, tag: @tag, next_tile: @next_tile, prev_tile: @prev_tile } %>

<%= render partial: 'explores/explore_intro' if @show_explore_intro %>