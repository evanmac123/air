<div id="prize_modal" class="reveal-modal" data-reveal data-raffle-show-start="<%= raffle.show_start?(current_user) %>" data-raffle-show-finish="<%= raffle.show_finish?(current_user) %>">
  <%= render partial: "client_admin/prizes/prize", locals: { raffle: raffle, entries: starting_tickets } %>
</div>
<% js_at_end do %>
  bindPrizeModal(<%= raffle.show_start? current_user %>, <%= raffle.show_finish? current_user %>);
<% end %>
