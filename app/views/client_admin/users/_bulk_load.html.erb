<%= form_tag @uploader.direct_fog_url, multipart: true do %>
  <%= hidden_field_tag 'key',                     @uploader.key %>
  <%= hidden_field_tag 'AWSAccessKeyId',          @uploader.aws_access_key_id %>
  <%= hidden_field_tag 'acl',                     @uploader.acl %>
  <%= hidden_field_tag 'success_action_redirect', @uploader.success_action_redirect %>
  <%= hidden_field_tag 'policy',                  @uploader.policy %>
  <%= hidden_field_tag 'signature',               @uploader.signature %>

  <%= file_field_tag 'file' %>
  <%= submit_tag 'Upload to S3' %>
<% end %>

<%= link_to "Download", "https://s3.amazonaws.com/airbo_downloadable_templates/Sample_Eligibility_File_name_and_email.csv" %>

<% if @demo.upload_in_progress %>
  Upload in progress. You can leave this page and we'll email you when it's complete.
<% end %>
