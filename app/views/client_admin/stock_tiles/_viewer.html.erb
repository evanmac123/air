<div class="viewer">
  <%= link_to "", client_admin_stock_tile_path(@prev), :id => :prev  %>
  <div id="slideshow">
    <%= render 'tiles/full_size_tile', :tile => @tile %>
  </div>
  <%= link_to "", client_admin_stock_tile_path(@next), :id => :next %>
</div>
