$("#follow_up_email_<%= @follow_up_email.id %>").remove();
if($('.follow_up_email_details').length == 0) {
  $("#cancel_follow_up").remove();
}

$('#feedback').html("Follow-up email for <%= @follow_up_email.send_on.to_time.to_s((:tile_digest_email_sent_at)) %> canceled");
