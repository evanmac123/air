%h2
  Summary
%table.survey-chart-table
  %thead
    %tr
      %th.answer
        Answer
      %th
        Number
      %th
        Percent
  %tbody
    - @survey_chart.each do |row|
      %tr
        %td
          = dispay_short_and_full_answer(row["answer"])
        %td
          = row["number"]
        %td
          = row["percent"]
