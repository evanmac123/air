= render "shared/flashes"

/ WE NEED DIGEST PAGE -- %div#digest=  render partial: 'digest'
/ WE ALSO NEED REPORTS LOOPED INTO THIS PAGE

/ %ul#tile_manager_nav


%section.manage_tiles#archived_tiles
  .row
    #tile_manager_nav.large-12.columns
      %ul.large-12
        %li#managing_tiles= link_to 'Manage tiles', client_admin_tiles_path
        %li#share_tiles= link_to 'Share tiles'
        %li#board_activity= link_to 'Board activity', client_admin_path
        %li.create_new_tile
          <a href="/client_admin/tiles/new">+ Add new tile</a>
    .large-12.columns
      %h1 Inactive tiles
      %div#archive= render partial: 'archive'

%section.manage_tiles#active_tiles
  .row
    .large-12.columns
      %h1 Currently live
      %div#active=  render partial: 'active'

- timeout_in_msecs = ThumbnailsController::THUMBNAIL_POLL_DELAY * 1000
- content_for :javascript do
  :javascript
    startTileThumbnailPollingFromQueue(#{timeout_in_msecs})
