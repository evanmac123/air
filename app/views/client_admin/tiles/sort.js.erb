replaceMovedTile("<%= @tile.id %>", "<%= single_tile_for_sort_js(@tile) %>");
updateShareTilesNumber("<%= tiles_to_be_sent(current_user) %>");
updateShowMoreDraftTilesButton("<%= @show_more_draft_tiles %>");
<% @last_tiles.each do |last_tile| %>
  fillInLastTile( "<%= last_tile.id %>", 
                  "<%= last_tile.status %>", 
                  "<%= single_tile_for_sort_js(last_tile) %>" );
<% end %>