<div class='admin'>
  <h2 class="first"><%= link_to "Admin", admin_path %> » Edit <%= @demo.name %></h2>

  <p class="explanation"><%= translate('.edit_explanation', :default => "Edit the %{name} demo", :name => @demo.name) %></p>

  <%= render 'admin/demos/form', :demo => @demo %>
</div>
