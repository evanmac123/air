<div class="hidden-form">
  <%= semantic_remote_form_for [:admin, @demo, User.new] do |form| %>
    <%= form.inputs do %>
      <%= form.input :name,  :label => "Name" %>
      <%= form.input :email, :label => "Email" %>
    <% end %>

    <%= form.buttons do %>
      <%= form.submit 'Submit' %>
    <% end %>
  <% end %>
</div>
