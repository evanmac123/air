<div class="hidden-form">
  <%= form_for [:admin, @demo, User.new] do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %>

    <%= form.label :email %>
    <%= form.email_field :email %>

    <%= form.submit 'Submit' %>
  <% end %>
</div>
