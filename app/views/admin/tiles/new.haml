.breadcrumb
  = link_to "Admin", admin_path
  = "»"
  = link_to @demo.name, admin_demo_path(@demo)
  = "»"
  = link_to "Tiles", admin_demo_tiles_path(@demo)
  = "»"
  New
%br

.admin
  %p Please be aware that after creating this tile, it may be a few minutes before it's actually available to players.
  = render 'form', :tile => @tile
