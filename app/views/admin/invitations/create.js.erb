$('#<%= dom_id(@user) %> .invite-link').html(
  <%= raw render(:partial => 'admin/invitations/new',
                 :locals  => { :user => @user }).to_json %>
);
$('#<%= dom_id(@user) %>').effect("highlight", {}, 1500);
