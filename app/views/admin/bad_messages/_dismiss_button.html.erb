<%= form_tag admin_bad_message_path(message), :method => :put, :id => "dismiss-#{dom_id(message)}" do %>
  <%= hidden_field_tag 'message[is_new]', false %>
  <%= hidden_field_tag 'message[on_watch_list]', false %>
  <%= submit_tag 'Dismiss' %>
<% end %>
