<%#TODO use the image from the board instead of this convention which assumes png with name of the topic%>

<%= link_to public_board_path(public_slug:board.public_slug), class: "topic" do %>
  <div class="topic_cell">
    <%= image_tag(board.cover_image.url) %>
    <div class="shadow_overlay"></div>
    <div class="text-container">
      <h1 class="title"><%= board.name %></h1>
      <h2 class="subtitle">This is the subtitle</h3>
    </div>
  </div>
<% end %>
