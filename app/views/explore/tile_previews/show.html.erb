<% content_for :head do %>
  <%= render 'shared/opengraph_meta_tags_tile', tile: @tile %>
<% end %>
<%= render partial: 'tile_preview', locals: { tile: @tile, tag: @tag, next_tile: @next_tile, prev_tile: @prev_tile, section: "Explore Digest" } %>
