<section class="manage_section channel_container channel_header">
  <div class="row">
    <div class="large-12 columns">
      <h4 class="explore-tag"><%= channel.name %></h4>
      <% if channel.description %>
        <p class="channel-description"><%= channel.description %></p>
      <% end %>
    </div>
  </div>
</section>
