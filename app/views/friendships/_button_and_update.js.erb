$('#follow-target').html(
  <%= raw render(new_button_path, :user => @user).to_json %>
);
$('#followers-count').text('<%= followers_count_phrase(@user) %>');
$('#following-count').text('<%= following_count_phrase(@user) %>');
