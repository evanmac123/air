$('#follow-target').html(
  <%= raw render('friendships/unfollow', :user => @user).to_json %>
);
$('#followers-count strong').text(<%= @user.followers_count %>);
$('#following-count strong').text(<%= @user.following_count %>);
