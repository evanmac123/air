<%# render partial: 'mailer/header' %>

<% # We send 'claimed' users to the main activities page; "unclaimed" users have to accept their invitation first %>
<%= @presenter.digest_email_heading %>
<%= @presenter.custom_message_if_present %>

<% @tiles.each do |tile| %>

    <%= tile.headline %>
  <%= tile.email_site_link %>
  <%= tile.supporting_content + "\n" if @presenter.follow_up_email %> +<%= tile.points %> <%= tile.question %>
<% end %>

  See Tiles
  <%= @general_site_link %>
  You won't have to log in. Works on mobile.

<%= render partial: 'shared/unsubscribe_footer' %>
