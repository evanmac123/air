Hello <%= @user.name %>,

Welcome to the game!

To log in to the site, you'll need to set your password, which you can do here:

<% url = edit_user_password_url(@user, :token => @user.confirmation_token, :protocol => (Rails.env.development? ? 'http' : 'https')) %>
<%= url %>

Thanks for joining!

Sincerely,
The HEngage team.

<%= render 'shared/unsubscribe_footer' %>
