<% board_switching_allowed = current_user.try(:can_switch_boards?) %>
<% nerf_links_with_login_modal = current_user.try(:nerf_links_with_login_modal?) %>

<% js_at_end do %>
  bindOffcanvasMenu(<%= board_switching_allowed %>);
  <% if nerf_links_with_login_modal %>
    nerfLinksWithLoginModal();
  <% end %>
<% end %>
