<% if Rails.env.test? %>
  <% $ASSISTLY_CALLED = true %>
<% else %>



  <script>
    new ASSISTLY.Widget({ 
      version: 1, 
      site: 'hengage.assistly.com', 
      port: '80', 
      type: 'email', 
      displayMode: 1,  //0 for popup, 1 for lightbox
      features: {  
      },  
      fields: { 
              ticket: { 
                      // desc: '',
  // labels_new: '',
  // priority: '',
  // subject: ''
              }, 
              interaction: { 
                      // email: '',
  // name: ''
              }, 
              email: { 
                      //subject: '', 
                      //body: '' 
              }, 
              customer: { 
                      // company: '',
  // desc: '',
  // first_name: '',
  // last_name: '',
  // title: ''
              } 
      } 
    }).render();

    /* Get rid of Assistly's email graphic and put some text in, like Tim
       Berners-Lee intended. */
    $('.assistly-widget a').attr("style", null).text('Contact Us');
  </script>
<% end %>
