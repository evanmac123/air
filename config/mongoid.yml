<% mongo_uri = ENV['MONGOHQ_URL'] || "mongodb://localhost:27017/health_#{Rails.env}" %>

development: &default
  sessions:
    default:
      uri: <%= mongo_uri %>

test:
  <<: *default

staging:
  <<: *default

production:
  <<: *default
