development: &default
  adapter: postgresql
  min_messages: warning
  database: airbo
  host: localhost
  encoding: utf8

test: &test
  <<: *default
  database: airbo_test

production:
  adapter: postgresql
  min_messages: warning
  database: production
  host: localhost
  encoding: utf8
